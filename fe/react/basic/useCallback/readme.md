# useCallback & useMemo
为性能优化而生的hooks

## 问题
- 父组件有多个状态，子组件依赖其中的一些状态
- 父组件重新渲染时，子组件也会重新渲染
  更新
  带来性能的浪费
  希望不相关的属性发生改变时，子组件不重新渲染
  解决方案：memo memorize 请记住我
  React.memo()
  用于包裹组件，当组件的props没有改变时，不会重新渲染组件
  属性比对